// Generated by CoffeeScript 1.10.0
(function() {
  var MetadataType, references;

  MetadataType = require('./metadata-type').MetadataType;

  references = {
    NONE: 'none',
    PARENTS: 'parents',
    PARENTSANDSIBLINGS: 'parentsandsiblings',
    CHILDREN: 'children',
    DESCENDANTS: 'descendants',
    ALL: 'all'
  };

  (function() {
    var key, results, val;
    results = [];
    for (key in MetadataType) {
      val = MetadataType[key];
      if (val !== 'structure') {
        results.push(references[key] = val);
      }
    }
    return results;
  })();

  exports.MetadataReferences = Object.freeze(references);

}).call(this);
